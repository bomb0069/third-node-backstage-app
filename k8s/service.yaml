apiVersion: v1
kind: Service
metadata:
  name: third-node-gitops
  namespace: third-node-gitops
  labels:
    app: third-node-gitops
    backstage.io/kubernetes-id: third-node-gitops
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: third-node-gitops
